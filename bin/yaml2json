#!/usr/bin/env node

const { resolve } = require('path')
const { yaml2json } = require('../lib/yaml2json')

const path = resolve(process.cwd(), process.argv[2])
  
const jsonOutput = JSON.stringify(yaml2json(path))

process.stdout.write(Buffer.from(jsonOutput))
